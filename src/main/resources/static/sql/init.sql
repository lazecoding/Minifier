DROP TABLE IF EXISTS `url_map_o`;

CREATE TABLE `url_map_o` (
  `conversion_code` varchar(11) binary NOT NULL,
  `full_url` varchar(765) NOT NULL,
  `ttl` bigint(20) NOT NULL,
  `create_time` timestamp NOT NULL DEFAULT NOW(),
  PRIMARY KEY (`conversion_code`)
) ENGINE = InnoDB;

DROP TABLE IF EXISTS `url_map_n`;

CREATE TABLE `url_map_n` (
  `conversion_code` varchar(11) binary NOT NULL,
  `full_url` varchar(765) NOT NULL,
  `ttl` bigint(20) NOT NULL,
  `create_time` timestamp NOT NULL DEFAULT NOW(),
  PRIMARY KEY (`conversion_code`)
) ENGINE = InnoDB;

DROP TABLE IF EXISTS `url_map_w`;

CREATE TABLE `url_map_w` (
  `conversion_code` varchar(11) binary NOT NULL,
  `full_url` varchar(765) NOT NULL,
  `ttl` bigint(20) NOT NULL,
  `create_time` timestamp NOT NULL DEFAULT NOW(),
  PRIMARY KEY (`conversion_code`)
) ENGINE = InnoDB;

DROP TABLE IF EXISTS `url_map_x`;

CREATE TABLE `url_map_x` (
  `conversion_code` varchar(11) binary NOT NULL,
  `full_url` varchar(765) NOT NULL,
  `ttl` bigint(20) NOT NULL,
  `create_time` timestamp NOT NULL DEFAULT NOW(),
  PRIMARY KEY (`conversion_code`)
) ENGINE = InnoDB;

DROP TABLE IF EXISTS `url_map_u`;

CREATE TABLE `url_map_u` (
  `conversion_code` varchar(11) binary NOT NULL,
  `full_url` varchar(765) NOT NULL,
  `ttl` bigint(20) NOT NULL,
  `create_time` timestamp NOT NULL DEFAULT NOW(),
  PRIMARY KEY (`conversion_code`)
) ENGINE = InnoDB;

DROP TABLE IF EXISTS `url_map_y`;

CREATE TABLE `url_map_y` (
  `conversion_code` varchar(11) binary NOT NULL,
  `full_url` varchar(765) NOT NULL,
  `ttl` bigint(20) NOT NULL,
  `create_time` timestamp NOT NULL DEFAULT NOW(),
  PRIMARY KEY (`conversion_code`)
) ENGINE = InnoDB;

DROP TABLE IF EXISTS `url_map_r`;

CREATE TABLE `url_map_r` (
  `conversion_code` varchar(11) binary NOT NULL,
  `full_url` varchar(765) NOT NULL,
  `ttl` bigint(20) NOT NULL,
  `create_time` timestamp NOT NULL DEFAULT NOW(),
  PRIMARY KEY (`conversion_code`)
) ENGINE = InnoDB;

DROP TABLE IF EXISTS `url_map_d`;

CREATE TABLE `url_map_d` (
  `conversion_code` varchar(11) binary NOT NULL,
  `full_url` varchar(765) NOT NULL,
  `ttl` bigint(20) NOT NULL,
  `create_time` timestamp NOT NULL DEFAULT NOW(),
  PRIMARY KEY (`conversion_code`)
) ENGINE = InnoDB;

DROP TABLE IF EXISTS `url_map_c`;

CREATE TABLE `url_map_c` (
  `conversion_code` varchar(11) binary NOT NULL,
  `full_url` varchar(765) NOT NULL,
  `ttl` bigint(20) NOT NULL,
  `create_time` timestamp NOT NULL DEFAULT NOW(),
  PRIMARY KEY (`conversion_code`)
) ENGINE = InnoDB;

DROP TABLE IF EXISTS `url_map_i`;

CREATE TABLE `url_map_i` (
  `conversion_code` varchar(11) binary NOT NULL,
  `full_url` varchar(765) NOT NULL,
  `ttl` bigint(20) NOT NULL,
  `create_time` timestamp NOT NULL DEFAULT NOW(),
  PRIMARY KEY (`conversion_code`)
) ENGINE = InnoDB;

DROP TABLE IF EXISTS `url_map_j`;

CREATE TABLE `url_map_j` (
  `conversion_code` varchar(11) binary NOT NULL,
  `full_url` varchar(765) NOT NULL,
  `ttl` bigint(20) NOT NULL,
  `create_time` timestamp NOT NULL DEFAULT NOW(),
  PRIMARY KEY (`conversion_code`)
) ENGINE = InnoDB;

DROP TABLE IF EXISTS `url_map_4`;

CREATE TABLE `url_map_4` (
  `conversion_code` varchar(11) binary NOT NULL,
  `full_url` varchar(765) NOT NULL,
  `ttl` bigint(20) NOT NULL,
  `create_time` timestamp NOT NULL DEFAULT NOW(),
  PRIMARY KEY (`conversion_code`)
) ENGINE = InnoDB;

DROP TABLE IF EXISTS `url_map_k`;

CREATE TABLE `url_map_k` (
  `conversion_code` varchar(11) binary NOT NULL,
  `full_url` varchar(765) NOT NULL,
  `ttl` bigint(20) NOT NULL,
  `create_time` timestamp NOT NULL DEFAULT NOW(),
  PRIMARY KEY (`conversion_code`)
) ENGINE = InnoDB;

DROP TABLE IF EXISTS `url_map_6`;

CREATE TABLE `url_map_6` (
  `conversion_code` varchar(11) binary NOT NULL,
  `full_url` varchar(765) NOT NULL,
  `ttl` bigint(20) NOT NULL,
  `create_time` timestamp NOT NULL DEFAULT NOW(),
  PRIMARY KEY (`conversion_code`)
) ENGINE = InnoDB;

DROP TABLE IF EXISTS `url_map_m`;

CREATE TABLE `url_map_m` (
  `conversion_code` varchar(11) binary NOT NULL,
  `full_url` varchar(765) NOT NULL,
  `ttl` bigint(20) NOT NULL,
  `create_time` timestamp NOT NULL DEFAULT NOW(),
  PRIMARY KEY (`conversion_code`)
) ENGINE = InnoDB;

DROP TABLE IF EXISTS `url_map_8`;

CREATE TABLE `url_map_8` (
  `conversion_code` varchar(11) binary NOT NULL,
  `full_url` varchar(765) NOT NULL,
  `ttl` bigint(20) NOT NULL,
  `create_time` timestamp NOT NULL DEFAULT NOW(),
  PRIMARY KEY (`conversion_code`)
) ENGINE = InnoDB;

DROP TABLE IF EXISTS `url_map_b`;

CREATE TABLE `url_map_b` (
  `conversion_code` varchar(11) binary NOT NULL,
  `full_url` varchar(765) NOT NULL,
  `ttl` bigint(20) NOT NULL,
  `create_time` timestamp NOT NULL DEFAULT NOW(),
  PRIMARY KEY (`conversion_code`)
) ENGINE = InnoDB;

DROP TABLE IF EXISTS `url_map_q`;

CREATE TABLE `url_map_q` (
  `conversion_code` varchar(11) binary NOT NULL,
  `full_url` varchar(765) NOT NULL,
  `ttl` bigint(20) NOT NULL,
  `create_time` timestamp NOT NULL DEFAULT NOW(),
  PRIMARY KEY (`conversion_code`)
) ENGINE = InnoDB;

DROP TABLE IF EXISTS `url_map_a`;

CREATE TABLE `url_map_a` (
  `conversion_code` varchar(11) binary NOT NULL,
  `full_url` varchar(765) NOT NULL,
  `ttl` bigint(20) NOT NULL,
  `create_time` timestamp NOT NULL DEFAULT NOW(),
  PRIMARY KEY (`conversion_code`)
) ENGINE = InnoDB;

DROP TABLE IF EXISTS `url_map_3`;

CREATE TABLE `url_map_3` (
  `conversion_code` varchar(11) binary NOT NULL,
  `full_url` varchar(765) NOT NULL,
  `ttl` bigint(20) NOT NULL,
  `create_time` timestamp NOT NULL DEFAULT NOW(),
  PRIMARY KEY (`conversion_code`)
) ENGINE = InnoDB;

DROP TABLE IF EXISTS `url_map_h`;

CREATE TABLE `url_map_h` (
  `conversion_code` varchar(11) binary NOT NULL,
  `full_url` varchar(765) NOT NULL,
  `ttl` bigint(20) NOT NULL,
  `create_time` timestamp NOT NULL DEFAULT NOW(),
  PRIMARY KEY (`conversion_code`)
) ENGINE = InnoDB;

DROP TABLE IF EXISTS `url_map_9`;

CREATE TABLE `url_map_9` (
  `conversion_code` varchar(11) binary NOT NULL,
  `full_url` varchar(765) NOT NULL,
  `ttl` bigint(20) NOT NULL,
  `create_time` timestamp NOT NULL DEFAULT NOW(),
  PRIMARY KEY (`conversion_code`)
) ENGINE = InnoDB;

DROP TABLE IF EXISTS `url_map_1`;

CREATE TABLE `url_map_1` (
  `conversion_code` varchar(11) binary NOT NULL,
  `full_url` varchar(765) NOT NULL,
  `ttl` bigint(20) NOT NULL,
  `create_time` timestamp NOT NULL DEFAULT NOW(),
  PRIMARY KEY (`conversion_code`)
) ENGINE = InnoDB;

DROP TABLE IF EXISTS `url_map_p`;

CREATE TABLE `url_map_p` (
  `conversion_code` varchar(11) binary NOT NULL,
  `full_url` varchar(765) NOT NULL,
  `ttl` bigint(20) NOT NULL,
  `create_time` timestamp NOT NULL DEFAULT NOW(),
  PRIMARY KEY (`conversion_code`)
) ENGINE = InnoDB;

DROP TABLE IF EXISTS `url_map_s`;

CREATE TABLE `url_map_s` (
  `conversion_code` varchar(11) binary NOT NULL,
  `full_url` varchar(765) NOT NULL,
  `ttl` bigint(20) NOT NULL,
  `create_time` timestamp NOT NULL DEFAULT NOW(),
  PRIMARY KEY (`conversion_code`)
) ENGINE = InnoDB;

DROP TABLE IF EXISTS `url_map_z`;

CREATE TABLE `url_map_z` (
  `conversion_code` varchar(11) binary NOT NULL,
  `full_url` varchar(765) NOT NULL,
  `ttl` bigint(20) NOT NULL,
  `create_time` timestamp NOT NULL DEFAULT NOW(),
  PRIMARY KEY (`conversion_code`)
) ENGINE = InnoDB;

DROP TABLE IF EXISTS `url_map_t`;

CREATE TABLE `url_map_t` (
  `conversion_code` varchar(11) binary NOT NULL,
  `full_url` varchar(765) NOT NULL,
  `ttl` bigint(20) NOT NULL,
  `create_time` timestamp NOT NULL DEFAULT NOW(),
  PRIMARY KEY (`conversion_code`)
) ENGINE = InnoDB;

DROP TABLE IF EXISTS `url_map_7`;

CREATE TABLE `url_map_7` (
  `conversion_code` varchar(11) binary NOT NULL,
  `full_url` varchar(765) NOT NULL,
  `ttl` bigint(20) NOT NULL,
  `create_time` timestamp NOT NULL DEFAULT NOW(),
  PRIMARY KEY (`conversion_code`)
) ENGINE = InnoDB;

DROP TABLE IF EXISTS `url_map_0`;

CREATE TABLE `url_map_0` (
  `conversion_code` varchar(11) binary NOT NULL,
  `full_url` varchar(765) NOT NULL,
  `ttl` bigint(20) NOT NULL,
  `create_time` timestamp NOT NULL DEFAULT NOW(),
  PRIMARY KEY (`conversion_code`)
) ENGINE = InnoDB;

DROP TABLE IF EXISTS `url_map_l`;

CREATE TABLE `url_map_l` (
  `conversion_code` varchar(11) binary NOT NULL,
  `full_url` varchar(765) NOT NULL,
  `ttl` bigint(20) NOT NULL,
  `create_time` timestamp NOT NULL DEFAULT NOW(),
  PRIMARY KEY (`conversion_code`)
) ENGINE = InnoDB;

DROP TABLE IF EXISTS `url_map_v`;

CREATE TABLE `url_map_v` (
  `conversion_code` varchar(11) binary NOT NULL,
  `full_url` varchar(765) NOT NULL,
  `ttl` bigint(20) NOT NULL,
  `create_time` timestamp NOT NULL DEFAULT NOW(),
  PRIMARY KEY (`conversion_code`)
) ENGINE = InnoDB;

DROP TABLE IF EXISTS `url_map_2`;

CREATE TABLE `url_map_2` (
  `conversion_code` varchar(11) binary NOT NULL,
  `full_url` varchar(765) NOT NULL,
  `ttl` bigint(20) NOT NULL,
  `create_time` timestamp NOT NULL DEFAULT NOW(),
  PRIMARY KEY (`conversion_code`)
) ENGINE = InnoDB;

DROP TABLE IF EXISTS `url_map_e`;

CREATE TABLE `url_map_e` (
  `conversion_code` varchar(11) binary NOT NULL,
  `full_url` varchar(765) NOT NULL,
  `ttl` bigint(20) NOT NULL,
  `create_time` timestamp NOT NULL DEFAULT NOW(),
  PRIMARY KEY (`conversion_code`)
) ENGINE = InnoDB;

DROP TABLE IF EXISTS `url_map_g`;

CREATE TABLE `url_map_g` (
  `conversion_code` varchar(11) binary NOT NULL,
  `full_url` varchar(765) NOT NULL,
  `ttl` bigint(20) NOT NULL,
  `create_time` timestamp NOT NULL DEFAULT NOW(),
  PRIMARY KEY (`conversion_code`)
) ENGINE = InnoDB;

DROP TABLE IF EXISTS `url_map_5`;

CREATE TABLE `url_map_5` (
  `conversion_code` varchar(11) binary NOT NULL,
  `full_url` varchar(765) NOT NULL,
  `ttl` bigint(20) NOT NULL,
  `create_time` timestamp NOT NULL DEFAULT NOW(),
  PRIMARY KEY (`conversion_code`)
) ENGINE = InnoDB;

DROP TABLE IF EXISTS `url_map_f`;

CREATE TABLE `url_map_f` (
  `conversion_code` varchar(11) binary NOT NULL,
  `full_url` varchar(765) NOT NULL,
  `ttl` bigint(20) NOT NULL,
  `create_time` timestamp NOT NULL DEFAULT NOW(),
  PRIMARY KEY (`conversion_code`)
) ENGINE = InnoDB;